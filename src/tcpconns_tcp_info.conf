# Configuration file for mapping struct tcp_info into TCPConns report.

# This can be generated from /usr/include/linux/tcp.h by the script
# contrib/make_tcpconns_tcp_info, followed by some editing.

# TODO(arielshaqed): Do just that^^^^^!

# NAME			DS_TYPE		TCPINFO_TYPE:OFFSET
perf_smoothed_rtt	GAUGE		uint32:72
perf_retransmits	GAUGE		uint32:40
